// This file sets up how Jest will run our tests
module.exports = {
    // Tells Jest to run tests in a Node.js environment (for backend code)
    testEnvironment: 'node',
    // Makes Jest show detailed test results (easier to read what’s happening)
    verbose: true,
};